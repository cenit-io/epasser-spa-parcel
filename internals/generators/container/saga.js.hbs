import { call, put, select, takeLatest } from 'redux-saga/effects';

// Individual exports for testing
export default function* {{ camelCase name }}Saga() {
  // See example in src/container/SignIn
}
