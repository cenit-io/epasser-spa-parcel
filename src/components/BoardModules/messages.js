/*
 * BoardModules Messages
 *
 * This contains all the text for the BoardModules component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.BoardModules';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Integrations',
  },
});
