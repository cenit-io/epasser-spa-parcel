/**
 *
 * Styles
 *
 */

const styles = (theme) => ({

  root: {
    minWidth: 350,
    padding: '10px 10px 0 0',
    display: 'table-cell',
  },

  card: {
    height: 340,
  }

});

export default styles;
