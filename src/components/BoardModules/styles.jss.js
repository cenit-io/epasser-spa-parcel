/**
 *
 * Styles
 *
 */

const styles = (theme) => ({

  root: {
    minWidth: 350,
    padding: '10px 10px 0 0',
    display: 'table-cell',
  },

  card: {
    height: 350,
  },

});

export default styles;
