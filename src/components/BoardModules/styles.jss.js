/**
 *
 * Styles
 *
 */

const styles = (theme) => ({

  root: (props) => ({
    width: props.width,
    padding: '10px 10px 0 0',
    display: 'table-cell',
  }),

  card: (props) => ({
    height: props.height,
  }),

});

export default styles;
