/*
 * BoardLogistics Messages
 *
 * This contains all the text for the BoardLogistics component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.BoardLogistics';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Logistics',
  },
});
