/*
 * BoardWorkflows Messages
 *
 * This contains all the text for the BoardWorkflows component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.BoardWorkflows';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Workflows',
  },
});
