/**
 *
 * Styles
 *
 */

const styles = (theme) => ({

  root: {
    position: 'fixed',
    right: 20,
    top: 66,
    zIndex: 2000,

    '&.embedded': {
      top: 6,
    },
  },

});

export default styles;
