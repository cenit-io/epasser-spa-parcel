/**
 *
 * Styles
 *
 */

const styles = (theme) => ({

  root: {
    // position: 'fixed',
    left: 10,
    top:5,
    // width: 'calc(100% - 20px)',
    width: '100%',
  },

});

export default styles;
