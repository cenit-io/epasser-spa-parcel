/*
 * SubMenuItem Messages
 *
 * This contains all the text for the SubMenuItem component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.SubMenuItem';

export default defineMessages({
  upcoming_feature: {
    id: `${scope}.upcoming_feature`,
    defaultMessage: 'Upcoming feature',
  },
});
