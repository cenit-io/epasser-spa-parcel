/*
 * UnInstall Messages
 *
 * This contains all the text for the UnInstall component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.actions.UnInstall';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'UnInstall',
  },
});
