/*
 * Apply Messages
 *
 * This contains all the text for the Apply component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.actions.Apply';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Apply',
  },
});
