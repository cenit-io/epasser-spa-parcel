/*
 * Reload Messages
 *
 * This contains all the text for the Reload component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.actions.Reload';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Reload',
  },
});
