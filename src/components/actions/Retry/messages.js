/*
 * Retry Messages
 *
 * This contains all the text for the Retry component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.actions.Retry';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Retry',
  },
});
