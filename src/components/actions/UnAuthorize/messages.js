/*
 * UnAuthorize Messages
 *
 * This contains all the text for the UnAuthorize component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.actions.UnAuthorize';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Unauthorize',
  },
});
