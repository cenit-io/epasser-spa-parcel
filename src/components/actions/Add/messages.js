/*
 * Add Messages
 *
 * This contains all the text for the Add component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.actions.Add';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Add',
  },
});
