/*
 * Show Messages
 *
 * This contains all the text for the Show component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.actions.Show';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Details',
  },
});
