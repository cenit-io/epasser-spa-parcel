/*
 * LinkProductsBox Messages
 *
 * This contains all the text for the LinkProductsBox component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.LinkProductsBox';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Products',
  },
});
