/*
 * SelectBoxFlowType Messages
 *
 * This contains all the text for the SelectBoxFlowType component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.SelectBoxFlowType';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Type',
  },
});
