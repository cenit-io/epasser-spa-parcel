/*
 * SelectBoxTopic Messages
 *
 * This contains all the text for the SelectBoxTopic component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.SelectBoxTopic';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Topic',
  },
});
