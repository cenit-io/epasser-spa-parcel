/*
 * SelectBoxChannel Messages
 *
 * This contains all the text for the SelectBoxChannel component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.SelectBoxChannel';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Channel',
  },
});
