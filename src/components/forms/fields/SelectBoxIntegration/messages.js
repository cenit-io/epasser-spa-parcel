/*
 * SelectBoxIntegration Messages
 *
 * This contains all the text for the SelectBoxIntegration component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.SelectBoxIntegration';

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: 'Integration',
  },
});
