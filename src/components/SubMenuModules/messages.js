/*
 * SubMenuModules Messages
 *
 * This contains all the text for the SubMenuModules component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.SubMenuModules';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Logistics',
  },
});
