/*
 * MainLayout Messages
 *
 * This contains all the text for the MainLayout container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'containers.MainLayout';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MainLayout container!',
  },
});
