/**
 *
 * Flows/settings
 *
 */

import { FlowsIcon } from '../../../../components/Icons';

import messages from './messages';

export default {
  id: 'Flows',
  icon: FlowsIcon,
  apiPath: 'flows',
  attrIds: 'flow_ids',
  messages,
};
