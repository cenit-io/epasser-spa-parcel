/*
 * Flows Messages
 *
 * This contains all the text for the Flows container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'containers.Flows';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Flows',
  },
});
