/*
 * Tasks Messages
 *
 * This contains all the text for the Tasks container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'containers.Tasks';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Tasks',
  },
});
