/**
 *
 * Styles
 *
 */

const styles = (theme) => ({
  root: {
    display: 'flex',
    height: '100%',
  },

  notify: {
    width: '100%',
    position: 'relative',
    right: 0,
    top: 0,
    margin: 5,
  },
});

export default styles;
