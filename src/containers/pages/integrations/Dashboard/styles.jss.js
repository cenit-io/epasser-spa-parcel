import { alpha } from "@material-ui/core/styles";

/**
 *
 * Styles
 *
 */

const styles = (theme) => ({
  root: {
    position: 'relative',
    left: 'calc(50% - 525px)',
  },
});

export default styles;
