/*
 * Orders Messages
 *
 * This contains all the text for the Orders container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'containers.Orders';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Orders',
  },
});
