/**
 *
 * Tenants/settings
 *
 */

import { TenantsIcon } from '../../../../components/Icons';

import messages from './messages';

export default {
  id: 'Tenants',
  icon: TenantsIcon,
  apiPath: 'tenants',
  attrIds: 'tenant_ids',
  messages,
};
